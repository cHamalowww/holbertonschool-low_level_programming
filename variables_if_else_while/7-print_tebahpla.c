#include <stdio.h>
#include<string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int hpla;

	for (hpla = 122; hpla >= 97; hpla--)
	{
		putchar(hpla);
	}
	putchar('\n');
	return (0);
}
